name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: Spoteyfa-Portable/app/package-lock.json
    
    - name: Install dependencies
      working-directory: Spoteyfa-Portable/app
      run: npm ci
    
    - name: Debug - List files before build
      working-directory: Spoteyfa-Portable/app
      run: dir
    
    - name: Build Windows executable
      working-directory: Spoteyfa-Portable/app
      run: |
        npm run build:win
        echo "Build completed, checking dist folder:"
        dir dist
        
    - name: List all dist contents
      working-directory: Spoteyfa-Portable/app
      run: |
        echo "Contents of dist folder:"
        dir /s dist
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: windows-build-output
        path: Spoteyfa-Portable/app/dist/
        
    - name: Upload specific exe files
      uses: actions/upload-artifact@v4
      with:
        name: windows-exe-files
        path: Spoteyfa-Portable/app/dist/*.exe
        if-no-files-found: ignore
    
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          Spoteyfa-Portable/app/dist/*.exe
          Spoteyfa-Portable/app/dist/*.zip
          Spoteyfa-Portable/app/dist/*.msi
        tag_name: ${{ github.ref_name }}
        name: "Apple Spotify Player ${{ github.ref_name }}"
        body: |
          🎵 **Apple Spotify Player - Windows Release**
          
          ## 📥 Download Options
          - **Portable EXE**: No installation required
          - **Installer (MSI)**: Traditional Windows installation
          - **ZIP Archive**: Manual extraction
          
          ## ✨ Features
          - 🍎 Apple-style Glassmorphism Design
          - 🎵 Full Spotify Integration with OAuth
          - 🌙 Dark/Light Mode Toggle
          - 🔒 Secure Credential Management
          - ⚡ Auto-hide & Always-on-top
          
          ## 🚀 Usage
          1. Download your preferred format
          2. Run directly (portable) or install (MSI)
          3. Follow the setup wizard for Spotify
          4. Enjoy your Apple-style music player!
          
          ## 🔧 Requirements
          - Windows 10/11 (x64)
          - Internet connection for Spotify
          - Spotify account (free or premium)
        draft: false
        prerelease: false
        fail_on_unmatched_files: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}