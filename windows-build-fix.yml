# Optimized Windows Build Strategy
# Replace the "Build application" step with this:

      - name: ⚡ Build application
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Building SPOTEYFA v0.3.0 for ${{ matrix.platform }}"
          cd 0.3.0
          npm run build
        shell: ${{ matrix.platform == 'win' && 'cmd' || 'bash' }}

# Alternative: Platform-specific steps
      - name: 🪟 Build Windows
        if: matrix.platform == 'win'
        shell: cmd
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd 0.3.0
          echo Building Windows version...
          npm run build:win

      - name: 🍎 Build macOS
        if: matrix.platform == 'mac'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd 0.3.0
          echo "Building macOS version..."
          npm run build:mac

      - name: 🐧 Build Linux
        if: matrix.platform == 'linux'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd 0.3.0
          echo "Building Linux version..."
          npm run build:linux